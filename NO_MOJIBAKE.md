Сделай правку, но учти риск mojibake.

Правила:
1. Все изменённые файлы должны остаться в UTF-8.
2. Если добавляешь или меняешь русские строки в коде и есть хоть малейший риск проблем с кодировкой, записывай их через Unicode escape (`\uXXXX`).
3. Перед финальным ответом обязательно:
   - открой все изменённые файлы и проверь их содержимое;
   - отдельно проверь только изменённые файлы на типовые сигнатуры mojibake;
   - если что-то найдено, исправь;
   - только после этого отдавай результат.


Не считай задачу завершённой, пока не выполнишь проверку changed files на mojibake.
Не используй кириллицу в новых строковых литералах без проверки; при сомнении используй `\u`-escape.
